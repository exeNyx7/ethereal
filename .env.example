# Railway Environment Variables
# Configure these in Railway dashboard under "Variables" tab

# Server Port (Railway auto-assigns)
PORT=4000

# SMTP Configuration (optional - uses Ethereal test if not set)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Frontend URL (set to your Vercel deployment)
NEXT_PUBLIC_FRONTEND_URL=https://ethereal-kappa-beige.vercel.app
